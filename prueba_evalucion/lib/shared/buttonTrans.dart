import 'package:flutter/material.dart';

class ButtonTrans extends StatelessWidget {
  const ButtonTrans({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}